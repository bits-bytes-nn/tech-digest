resources:
  project_name: tech-digest
  stage: dev # dev or prod
  profile_name: # profile name (if you want to use a specific profile)
  default_region_name: ap-northeast-2
  bedrock_region_name: us-west-2
  s3_bucket_name: # s3 bucket name
  s3_prefix: tech-digest
  vpc_id: # vpc id (if you want to use a specific vpc)
  subnet_ids: # list of subnet ids (if you want to use a specific subnet)
  lambda_or_batch: batch # lambda or batch
  cron_expression: "cron(0 1 ? * 6 *)"

scraping:
  rss_urls:
    - "https://medium.com/feed/airbnb-engineering"
    - "https://www.amazon.science/index.rss"
    - "https://www.anthropic.com/news"
    - "https://aws.amazon.com/blogs/amazon-ai/feed/"
    - "https://deepmind.google/blog/rss.xml" # was www.deepmind.com/blog/rss.xml (302 redirect)
    - "https://research.google/blog"
    - "https://huggingface.co/blog/feed.xml"
    - "https://tech.kakao.com/feed/"
    - "https://www.linkedin.com/blog/engineering"
    - "https://ai.meta.com/blog/"
    - "https://engineering.fb.com/feed/" # was code.facebook.com/posts/rss (301 redirect)
    - "https://www.microsoft.com/en-us/research/blog/feed/" # was research.microsoft.com/rss/news.xml (301 redirect)
    - "https://medium.com/feed/netflix-techblog" # was techblog.netflix.com (301 redirect to Medium)
    - "https://developer.nvidia.com/blog/feed"
    - "https://openai.com/news/rss.xml"
    - "https://medium.com/feed/@palantir"
    - "https://medium.com/feed/@Pinterest_Engineering"
    - "https://qwenlm.github.io/blog/"
    - "https://x.ai/news"
    - "https://eugeneyan.com/rss/"
    - "https://sebastianraschka.com/rss_feed.xml"
    - "https://www.philschmid.de/rss"
  days_back: 7
  # Minimum visible-text length (markup stripped) for a post to be summarized.
  # Posts below this are reported as filtered-out, not summarized.
  min_content_length: 600
  # Sources whose fetch failure is KNOWN and expected, matched as a substring of
  # the configured URL. They still appear in the crawl-health report but do not
  # raise the SNS alert. x.ai (and sometimes ai.meta.com) reject AWS datacenter
  # egress IPs on essentially every run; alerting weekly on a failure nobody can
  # act on trains you to ignore the alarm, which then hides a real breakage.
  expected_flaky_urls:
    - "x.ai/news"

summarization:
  use_filtering: True
  filtering_criteria: all
  included_topics: # list of included topics
  excluded_topics: # list of excluded topics
  filtering_model_id: anthropic.claude-sonnet-5
  filtering_enable_thinking: False
  summarization_model_id: anthropic.claude-sonnet-5
  summarization_enable_thinking: False
  greeting_model_id: anthropic.claude-haiku-4-5-20251001-v1:0
  max_concurrency: 5
  min_score: 0.8
  # Keep max_posts modest: mail clients truncate a message over ~100 KB, and each
  # article card costs roughly 12-16 KB. The build logs the rendered size and
  # warns when an issue would be clipped.
  max_posts: 5
  # Optional cap on articles from any single source, applied after score ranking;
  # slots the cap frees are backfilled in rank order, so it changes WHICH posts
  # ship, never how many. Leave blank for a single-vendor digest
  # (filtering_criteria: amazon) where source concentration is the point; set 2
  # for a broad digest, where three cards from one blog reads like that vendor's
  # release notes rather than a curated issue.
  max_per_source: 2
  # Send the relevance filter the article's visible TEXT instead of its raw HTML.
  # Measured on a real run, markup was ~87% of filtering input tokens (one page:
  # 77,417 chars of HTML around 5,669 chars of article) and filtering is ~74% of
  # this pipeline's Bedrock spend, so this cut cost per run 48%. The rubric scores
  # topic and quality, which live in the prose. Summarization still gets the full
  # HTML (it needs code blocks, tables and image URLs). Set False to revert.
  filter_on_visible_text: True
  # Extended-thinking budgets (tokens), used only when *_enable_thinking is True.
  # Must be >= 1024 and < the model's max output tokens (auto-clamped otherwise).
  # NOTE: adaptive-thinking models (Sonnet 5+) IGNORE these budgets — they are
  # steered by `thinking_effort` below. The budgets apply only to legacy
  # budget-thinking models (Sonnet 4.x / older).
  filtering_thinking_budget_tokens: 4096
  summarization_thinking_budget_tokens: 8192
  # Adaptive-thinking depth for newer models (Sonnet 5+ / Opus 5):
  #   low | medium | high | xhigh | max
  # Blank = model default. "xhigh" and "max" are Opus-tier only. Ignored by
  # legacy budget-thinking models (Sonnet 4.x and older).
  thinking_effort:
  # Optional cap on summary output tokens (blank = use the model's maximum).
  summarization_max_tokens:

newsletter:
  send_emails: True
  save_articles: False
  convert_to_images: False
  sender: "example@example.com" # email address of the sender
  header_title: "Weekly AI Tech Blog Digest"
  header_description: '$ cron weekly-digest | grep "AI tech" | sort -by relevance > your-inbox 🔮'
  header_thumbnail: "https://cdn.jsdelivr.net/gh/youngmki/playground@main/tech-digest-assets/images/peccy.png"
  footer_title: "Thank you for becoming a subscriber to Weekly AI Tech Blog Digest.<br>If you have any questions or feedback, please send them to <a href='mailto:example@example.com' style='color: #007bff; text-decoration: none;'>example@example.com</a>." # footer title
  logos:
    airbnb: "https://cdn.jsdelivr.net/gh/youngmki/playground@main/tech-digest-assets/images/airbnb.png"
    amazon: "https://cdn.jsdelivr.net/gh/youngmki/playground@main/tech-digest-assets/images/amazon.png"
    anthropic: "https://cdn.jsdelivr.net/gh/youngmki/playground@main/tech-digest-assets/images/anthropic.png"
    aws: "https://cdn.jsdelivr.net/gh/youngmki/playground@main/tech-digest-assets/images/aws.png"
    google: "https://cdn.jsdelivr.net/gh/youngmki/playground@main/tech-digest-assets/images/google.png"
    huggingface: "https://cdn.jsdelivr.net/gh/youngmki/playground@main/tech-digest-assets/images/huggingface.png"
    kakao: "https://cdn.jsdelivr.net/gh/youngmki/playground@main/tech-digest-assets/images/kakao.png"
    linkedin: "https://cdn.jsdelivr.net/gh/youngmki/playground@main/tech-digest-assets/images/linkedin.png"
    meta: "https://cdn.jsdelivr.net/gh/youngmki/playground@main/tech-digest-assets/images/meta.png"
    microsoft: "https://cdn.jsdelivr.net/gh/youngmki/playground@main/tech-digest-assets/images/microsoft.png"
    ncsoft: "https://cdn.jsdelivr.net/gh/youngmki/playground@main/tech-digest-assets/images/ncsoft.png"
    netflix: "https://cdn.jsdelivr.net/gh/youngmki/playground@main/tech-digest-assets/images/netflix.png"
    nvidia: "https://cdn.jsdelivr.net/gh/youngmki/playground@main/tech-digest-assets/images/nvidia.png"
    openai: "https://cdn.jsdelivr.net/gh/youngmki/playground@main/tech-digest-assets/images/openai.png"
    palantir: "https://cdn.jsdelivr.net/gh/youngmki/playground@main/tech-digest-assets/images/palantir.png"
    pinterest: "https://cdn.jsdelivr.net/gh/youngmki/playground@main/tech-digest-assets/images/pinterest.png"
    qwen: "https://cdn.jsdelivr.net/gh/youngmki/playground@main/tech-digest-assets/images/qwen.png"
    xai: "https://cdn.jsdelivr.net/gh/youngmki/playground@main/tech-digest-assets/images/xai.png"
    unknown: "https://cdn.jsdelivr.net/gh/youngmki/playground@main/tech-digest-assets/images/unknown.png"
    # A source with no entry here falls back to `unknown` automatically, so a
    # newly mapped source (e.g. eugene_yan, phil_schmid, sebastian_raschka)
    # renders fine without a logo asset — it just shows the placeholder.
    # NOTE: add new logos here
